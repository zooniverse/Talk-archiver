version: '3.7'

services:
  talk-archiver:
    build:
      context: .
    volumes:
      - node_modules:/app/node_modules
      - api_data_cache:/app/cache
      - ./:/app
    ports:
      - "8080:8080"

volumes:
  node_modules:
  api_data_cache: